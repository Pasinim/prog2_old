package giu2021_battagliaNavale.V2;
import java.util.*;

public class TestRunner {
    public static void main(String[] args) {
        Osservatore o = new Osservatore();
        System.out.println(o.toString());
        

        // Set<Nave> s =  new HashSet<>();
        // Coordinata c = new Coordinata(0, 0);
        // Coordinata c1 = new Coordinata(1, 0);
        // Nave n = new Nave(Tipo.C, true, c);
        // s.add(n);
        // Giocatore g1 = new Giocatore(s);
        // Giocatore g2 = new Giocatore(s); //stessa flotta per entrambi i giocatori
        // //System.out.println(g2.getGrigliaFlotta().toString());
        // g1.colpisci(c, g2.getGrigliaFlotta());
        // g1.colpisci(c1, g2.getGrigliaFlotta());
        // System.out.println(g1.toString());


    }
}
