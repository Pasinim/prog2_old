package set2020_bancarellel;
/**
 * OVERVIEW: Questa classe rappresenta l'inventario di una bancarelle.
 * La classe è mutabile
 * ABS FUN: AF(items, quantity) -> (items[i] : quantity[i])
 * INV REP: items != null, quantity != null
 *          Per ogni i, j dove i!=j items[i] != items[j]
 *          size(items) == size(quantity)
 *
 */

public class Inventario {
    
}
