package Giu2020_Teramini;

public class ComparatoreCoord implements Comparator<Coordinata>{
    @Override
    //quando una coordinata è più grande dell'alra?
    public int compara(Coordinata a, Coordinata b){
        if (a.getX() > b.getX()) 
    }
}
